# Personal self-hosted web app

We will build the tracker as a web/PWA application backed by a personal self-hosted server rather than as separate native clients or a purely local app. This gives Linux, macOS, and Android a single interface while keeping calendar synchronization, background jobs, and encrypted provider credentials on infrastructure controlled by the user.
