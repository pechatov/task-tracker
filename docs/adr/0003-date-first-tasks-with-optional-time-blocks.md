# Date-first tasks with optional time blocks

Tasks are date-first in the MVP because most planning starts by assigning work to a day, not to an exact time. The task model will still allow an optional time block so a task can later occupy a calendar slot and support drag-and-drop rescheduling without changing the core task concept.
