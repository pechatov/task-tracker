# Single-user MVP with user-scoped data

The MVP will run as a single-user app, but the data model will be user-scoped from the start. This keeps the first release simple while avoiding a later migration where tasks, streams, projects, connected calendars, and sync credentials must be retrofitted with ownership.
