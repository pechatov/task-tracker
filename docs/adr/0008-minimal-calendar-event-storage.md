# Minimal calendar event storage

The MVP will store only the calendar event fields needed for planning, plus a browser-openable event link when the provider supplies one, and will not persist full event bodies or descriptions by default. This reduces the amount of sensitive corporate and personal calendar data stored on the self-hosted server while still allowing the user to jump to the source event for details.
